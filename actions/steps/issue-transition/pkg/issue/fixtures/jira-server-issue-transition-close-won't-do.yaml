connection:
  url: ${RELAY_JIRA_SERVER_URL}
  username: ${RELAY_JIRA_SERVER_USERNAME}
  password: ${RELAY_JIRA_SERVER_PASSWORD}
issue:
  key:
    RELAY-44
  fields:
    status:
      name: Close Issue
    resolution:
      name: "Won't Do"
